[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "firecrawl-py>=4.28.2",
    "torch>=2.12.0",
    "transformers>=5.12.1",
]
